# JAVIS DRIVER PREMIUM — ENVIRONMENT CONFIGURATION TEMPLATE
# No secrets or private keys are hardcoded in the application.

# Environment
APP_ENV=development
LOG_LEVEL=debug

# Optional External Corridor Sync Service (Disabled by default for local-first MVP)
CORRIDOR_API_BASE_URL=
CORRIDOR_API_KEY=
